LOCAL_HOST = '127.0.0.1'


class TestServer:
    async def test(self):
        # port = 10001
        # AsyncBase.coro2task_exec(server_main(LOCAL_HOST, port))
        # await asyncio.sleep(1)
        # # 调用不存在的服务
        # assert FuncTool.is_await_err(TcpApiManage.service(LOCAL_HOST, port, ''))
        pass
    pass
